-- SELECT country as country,
--        device_type as device_type,
--        revenue as revenue,
--        pageviews as pageviews,
--        sessions as sessions


-- FROM
-- (SELECT "US" as country, "Desktop" as device_type, 100 as revenue, 1000 as pageviews, 500 as sessions),


${generate_random_datasets.render_script()}